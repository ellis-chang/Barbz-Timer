/*const toggleMenuPath = require('../scripts/menu');

describe('Nav menu is ', () => {

    test('clicked', () => {
        expect(menuPath(100)).toContain('3');
    });

})*/

test('test', () => {
    expect(true).toBe(true);
})